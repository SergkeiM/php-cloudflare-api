# Networks

> The private network reachable through Cloudflare Tunnel: the routes that say which CIDRs a tunnel serves, and the virtual networks that let overlapping ranges coexist.

The private network reachable through Cloudflare Tunnel: the routes that say
which CIDRs a tunnel serves, and the virtual networks that let overlapping
ranges coexist.

## Related

- [Routes](/client/zero-trust/networks/routes)
- [Virtual Networks](/client/zero-trust/networks/virtual-networks)
