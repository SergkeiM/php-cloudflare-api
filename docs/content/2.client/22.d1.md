---
title: D1
description: "D1 endpoint reference."
navigation:
    title: D1
---

## List

Returns a list of D1 databases.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "params"
    type: "array"
    required: false
    description: "Query Parameters."
    default: "[]"
---
::

```php [php]
$response = $client->d1()->list('ACCOUNT_ID', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/list/"}
View this operation on the Cloudflare API Reference
::

## Create

Create D1 Database

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "name"
    type: "string"
    required: true
    description: "Database name Match pattern: `^[a-z0-9][a-z0-9-_]*$`"
  - name: "location"
    type: "string|null"
    required: false
    description: "Specify the region to create the D1 primary, if available. If this option is omitted, the D1 will be created as close as possible to the current user. Allowed values: `wnam`,`enam`,`weur`,`eeur`,`apac`,`oc`"
---
::

```php [php]
$response = $client->d1()->create('ACCOUNT_ID', 'NAME', 'LOCATION');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/create/"}
View this operation on the Cloudflare API Reference
::

## Get

Returns the specified D1 database.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
---
::

```php [php]
$response = $client->d1()->get('ACCOUNT_ID', 'DATABASE_ID');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/get/"}
View this operation on the Cloudflare API Reference
::

## Delete

Deletes the specified D1 database.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
---
::

```php [php]
$response = $client->d1()->delete('ACCOUNT_ID', 'DATABASE_ID');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/delete/"}
View this operation on the Cloudflare API Reference
::

## Update

Updates the specified D1 database, overwriting the full configuration.

```php
$client->d1()->update('ACCOUNT_ID', 'DATABASE_ID', [
    'read_replication' => ['mode' => 'auto'],
]);
```

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "values"
    type: "array"
    required: true
    description: "`read_replication` is required, itself carrying a `mode` of `auto` to let D1 place replicas around the world, or `disabled` to use none."
---
::

```php [php]
$response = $client->d1()->update('ACCOUNT_ID', 'DATABASE_ID', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/d1/best-practices/read-replication/"}
View this operation on the Cloudflare API Reference
::

## Edit

Applies changes to the specified D1 database, overwriting only the supplied properties.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "values"
    type: "array"
    required: true
    description: "Values to patch on the database, e.g. `read_replication`."
---
::

```php [php]
$response = $client->d1()->edit('ACCOUNT_ID', 'DATABASE_ID', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/d1/best-practices/read-replication/"}
View this operation on the Cloudflare API Reference
::

## Bookmark

Get a Time Travel bookmark for a D1 database.

Without a timestamp you get the current bookmark; with one, the nearest
bookmark at or before it. Feed either into `restore()`.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "timestamp"
    type: "string|null"
    required: false
    description: "ISO 8601 timestamp to find the nearest bookmark at or before. Omit for the current bookmark."
---
::

```php [php]
$response = $client->d1()->bookmark('ACCOUNT_ID', 'DATABASE_ID', 'TIMESTAMP');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/d1/reference/time-travel/"}
View this operation on the Cloudflare API Reference
::

## Restore

Restore a D1 database to a previous point in time.

Give it exactly one of a bookmark or a timestamp — a bookmark from
`bookmark()` names an exact point, a timestamp asks D1 to find the
nearest one.

```php
$bookmark = $client->d1()->bookmark('ACCOUNT_ID', 'DATABASE_ID')->json('result.bookmark');

$client->d1()->restore('ACCOUNT_ID', 'DATABASE_ID', bookmark: $bookmark);
```

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "bookmark"
    type: "string|null"
    required: false
    description: "Bookmark to restore to. Required if `$timestamp` is not given."
  - name: "timestamp"
    type: "string|null"
    required: false
    description: "ISO 8601 timestamp to restore to. Required if `$bookmark` is not given."
---
::

```php [php]
$response = $client->d1()->restore('ACCOUNT_ID', 'DATABASE_ID', 'BOOKMARK', 'TIMESTAMP');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/d1/reference/time-travel/"}
View this operation on the Cloudflare API Reference
::

## Export

Returns a URL where the SQL contents of your D1 can be downloaded. Note: this process may take some time for larger DBs, during which your D1 will be unavailable to serve queries. To avoid blocking your DB unnecessarily, an in-progress export must be continually polled or will automatically cancel.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "currentBookmark"
    type: "string|null"
    required: false
    description: "To poll an in-progress export, provide the current bookmark (returned by your first polling response)"
  - name: "noData"
    type: "bool"
    required: false
    description: "Export only the table definitions, not their contents"
    default: "false"
  - name: "noSchema"
    type: "bool"
    required: false
    description: "Export only each table's contents, not its definition"
    default: "false"
  - name: "tables"
    type: "array"
    required: false
    description: "Filter the export to just one or more tables. Passing an empty array is the same as not passing anything and means: export all tables."
    default: "[]"
---
::

```php [php]
$response = $client->d1()->export('ACCOUNT_ID', 'DATABASE_ID', 'CURRENT_BOOKMARK', true, true, []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/export/"}
View this operation on the Cloudflare API Reference
::

## Import

Generates a temporary URL for uploading an SQL file to, then instructing the D1 to import it and polling it for status updates. Imports block the D1 for their duration.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "action"
    type: "string"
    required: false
    description: "- `init`: Indicates you have a new SQL file to upload. - `ingest`: Indicates you've finished uploading to tell the D1 to start consuming it - `poll`: Indicates you've finished uploading to tell the D1 to start consuming it"
    default: "'init'"
  - name: "etag"
    type: "string|null"
    required: false
    description: "Required when action is `init` or `ingest`. An md5 hash of the file you're uploading. Used to check if it already exists, and validate its contents before ingesting."
  - name: "filename"
    type: "string|null"
    required: false
    description: "The filename you have successfully uploaded."
  - name: "currentBookmark"
    type: "string|null"
    required: false
    description: "This identifies the currently-running import, checking its status."
---
::

```php [php]
$response = $client->d1()->import('ACCOUNT_ID', 'DATABASE_ID', 'ACTION', 'ETAG', 'FILENAME', 'CURRENT_BOOKMARK');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/import/"}
View this operation on the Cloudflare API Reference
::

## Query

Returns the query result as an object.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "query"
    type: "string"
    required: true
    description: "Your SQL query. Supports multiple statements, joined by semicolons, which will be executed as a batch."
  - name: "params"
    type: "array"
    required: false
    description: "Query params."
    default: "[]"
---
::

```php [php]
$response = $client->d1()->query('ACCOUNT_ID', 'DATABASE_ID', 'QUERY', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/query/"}
View this operation on the Cloudflare API Reference
::

## Raw

Returns the query result rows as arrays rather than objects. This is a performance-optimized version of the /query endpoint.

::params-table
---
params:
  - name: "accountId"
    type: "string"
    required: true
    description: "Account Identifier."
  - name: "databaseId"
    type: "string"
    required: true
    description: "Database Identifier."
  - name: "query"
    type: "string"
    required: true
    description: "Your SQL query. Supports multiple statements, joined by semicolons, which will be executed as a batch."
  - name: "params"
    type: "array"
    required: false
    description: "Query params."
    default: "[]"
---
::

```php [php]
$response = $client->d1()->raw('ACCOUNT_ID', 'DATABASE_ID', 'QUERY', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/raw/"}
View this operation on the Cloudflare API Reference
::

