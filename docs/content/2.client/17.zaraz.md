---
title: Zaraz
description: "Zaraz: third-party tool loading, configured per zone and served from the edge."
navigation:
    title: Zaraz
---

Zaraz: third-party tool loading, configured per zone and served from the edge.

## Get Config

Get the latest Zaraz configuration for a zone.

Returns whichever of the preview or published configuration was updated
last. Values of secret variables are omitted — use `export()` to get
them.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
---
::

```php [php]
$response = $client->zaraz()->getConfig('ZONE_ID');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/config/methods/get/"}
View this operation on the Cloudflare API Reference
::

## Update Config

Update the Zaraz configuration for a zone.

This writes the preview configuration; call `publish()` to make it live.
Cloudflare replaces the whole configuration, so send a complete one —
read the current config, change what you need, and send it back.

```php
$config = $client->zaraz()->getConfig('ZONE_ID')->json('result');
$config['debugKey'] = 'new-debug-key';

$client->zaraz()->updateConfig('ZONE_ID', $config);
```

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
  - name: "values"
    type: "array"
    required: true
    description: "The complete configuration: `dataLayer`, `debugKey`, `settings`, `triggers`, `variables`, `tools` and `zarazVersion` are all required, with `analytics`, `consent` and `historyChange` optional."
---
::

```php [php]
$response = $client->zaraz()->updateConfig('ZONE_ID', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/config/methods/update/"}
View this operation on the Cloudflare API Reference
::

## Get Default

Get the default Zaraz configuration for a zone.

The configuration a zone starts from, useful as a baseline to reset to.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
---
::

```php [php]
$response = $client->zaraz()->getDefault('ZONE_ID');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/default/methods/get/"}
View this operation on the Cloudflare API Reference
::

## Export

Export the published Zaraz configuration for a zone.

Unlike `getConfig()`, this returns the published configuration in full,
secret variable values included.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
---
::

```php [php]
$response = $client->zaraz()->export('ZONE_ID');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/export/methods/get/"}
View this operation on the Cloudflare API Reference
::

## History

List published Zaraz configuration records for a zone.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
  - name: "params"
    type: "array"
    required: false
    description: "Query Parameters: `offset` (default `0`), `limit` (default `10`), `sortField` (one of `id`, `user_id`, `description`, `created_at`, `updated_at`) and `sortOrder` (`DESC` or `ASC`)."
    default: "[]"
---
::

```php [php]
$response = $client->zaraz()->history('ZONE_ID', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/history/methods/list/"}
View this operation on the Cloudflare API Reference
::

## Restore

Restore a published Zaraz configuration by ID.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
  - name: "configId"
    type: "int"
    required: true
    description: "ID of the historical configuration to restore, as listed by `history()`."
---
::

```php [php]
$response = $client->zaraz()->restore('ZONE_ID', 1);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/history/methods/update/"}
View this operation on the Cloudflare API Reference
::

## Configs

Get published Zaraz configurations by ID.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
  - name: "ids"
    type: "array"
    required: true
    description: "IDs of the historical configurations to fetch."
---
::

```php [php]
$response = $client->zaraz()->configs('ZONE_ID', []);
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/history/subresources/configs/methods/get/"}
View this operation on the Cloudflare API Reference
::

## Get Workflow

Get the Zaraz workflow for a zone.

The workflow decides which configuration visitors are served: `realtime`
serves the published one, `preview` serves the one you are editing.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
---
::

```php [php]
$response = $client->zaraz()->getWorkflow('ZONE_ID');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/workflow/methods/get/"}
View this operation on the Cloudflare API Reference
::

## Update Workflow

Update the Zaraz workflow for a zone.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
  - name: "workflow"
    type: "string"
    required: true
    description: "Either `realtime` or `preview`."
---
::

```php [php]
$response = $client->zaraz()->updateWorkflow('ZONE_ID', 'WORKFLOW');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/methods/update/"}
View this operation on the Cloudflare API Reference
::

## Publish

Publish the current Zaraz preview configuration for a zone.

::params-table
---
params:
  - name: "zoneId"
    type: "string"
    required: true
    description: "Zone Identifier."
  - name: "description"
    type: "string"
    required: false
    description: "Description recorded against the published configuration, and shown in `history()`."
    default: "''"
---
::

```php [php]
$response = $client->zaraz()->publish('ZONE_ID', 'DESCRIPTION');
```

::callout{icon="i-simple-icons-cloudflare" to="https://developers.cloudflare.com/api/resources/zaraz/subresources/publish/methods/create/"}
View this operation on the Cloudflare API Reference
::

